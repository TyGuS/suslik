should be able to convert a singly-linked list to doubly-linked list

finger-grained immutability
how can we propagate it?
#######

{ s =i {v} ++ s1 ; [[x, 3]] ** [x :-> v] ** [(x + 1) :-> nxt] ** (x + 2) :-> extra ** lseg_extra(nxt, s1) }
void sll_to_dll(loc x)
{ true ; dllseg(x, z, s)}

#######

void sll_to_dll (loc x) {
  if (x == 0) {
  } else {
    let nxt2 = *(x + 1);
    sll_to_dll(nxt2);
    set_prev(nxt2, x);
  }
}