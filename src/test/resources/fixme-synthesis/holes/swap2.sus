/* swap with holes 2 */

void swap(loc x, loc y)
{ x :-> a ** y :-> b }
{ x :-> b ** y :-> a }
{
  let yy = *y;
  let xx = *x;
  ??
}

###

void swap (loc x, loc y) {
  let yy = *y;
  let xx = *x;
  *x = yy;
  *y = xx;
}