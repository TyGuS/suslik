/* read doesn't redefine values */

void swap(loc x, loc y)
{ x :-> a ** y :-> b }
{ x :-> b ** y :-> a }
{
  let yy = *y;
  let xx = *x;
  let yy = *x;
  ??
}

###
void swap (loc x, loc y) {
  let yy = *y;
  let xx = *x;
  error;
}