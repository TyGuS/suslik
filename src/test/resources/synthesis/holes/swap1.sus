/* swap with holes 1 */

void swap(loc x, loc y)
{ x :-> a ** y :-> b }
{ x :-> b ** y :-> a }
{
  let yy = *y;
  ??
}

###

void swap (loc x, loc y) {
  let yy = *y;
  let a = *x;
  *x = yy;
  *y = a;
}