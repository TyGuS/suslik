predicate lseg(x, y) {
|  x == y        => {emp}
|  not (x == y)  => {[x, 2] ** x :-> 0 ** (x + 1) :-> nxt ** lseg(nxt, y)}
}

predicate lseg1(x, y) {
|  x == y        => {emp}
|  not (x == y)  => {[x, 2] ** x :-> 1 ** (x + 1) :-> nxt ** lseg1(nxt, y)}
}
