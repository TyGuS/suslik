should be able to deallocate a tree from a pointer to the head
###

{true; r :-> x ** tree0(x)}
  void free_tree(loc r, loc x)
{true ; r :-> 0 }

###

void free_tree (loc r, loc x) {
  if (x == 0) {
  } else {
    let l2 = *(x + 1);
    let r12 = *(x + 2);
    *r = l2;
    free_tree(r, l2);
    *r = r12;
    free_tree(r, r12);
    free(x);
    *r = 0;
  }
}