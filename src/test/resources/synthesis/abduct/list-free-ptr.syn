should be able to deallocate a list from the pointer to a head
###

{true; r :-> x ** lseg(x, 0, n)}
  void list_free(loc r, loc x)
{true ; r :-> 0}

###

void list_free (loc r, loc x) {
  if (x == 0) {
  } else {
    let nxt2 = *(x + 1);
    *r = nxt2;
    list_free(r, nxt2);
    free(x);
    *r = 0;
  }
}