# -x true
Deallocate three linked lists

###

{true; sll(x, s1)<a> ** sll(y, s2)<b> ** sll(z, s3)<c>}
  void listfree3(loc x, loc y, loc z)
{true ; emp }

###

{true ; sll(x, s1)<a> ** sll(y, s2)<b> ** sll(z, s3)<c>}
{true ; emp}
void listfree3 (loc x, loc y, loc z) {
  if (x == 0) {
    listfree300(x, y, z);
  } else {
    let n = *(x + 1);
    listfree3(z, y, n);
    free(x);
  }
}

{a == 0 && s1 =i {} && x == 0 ; sll(y, s2)<b> ** sll(z, s3)<c>}
{true ; emp}
void listfree300 (loc x, loc y, loc z) {
  if (y == 0) {
    listfree30001(x, y, z);
  } else {
    let n = *(y + 1);
    listfree300(x, z, n);
    free(y);
  }
}

{a == 0 && s1 =i {} && x == 0 ; sll(z, s3)<c>}
{true ; emp}
void listfree30001 (loc x, loc y, loc z) {
  if (z == 0) {
  } else {
    let n = *(z + 1);
    listfree30001(x, y, n);
    free(z);
  }
}
