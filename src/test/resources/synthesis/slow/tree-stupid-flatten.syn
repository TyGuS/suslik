should be able to flatten a tree
###

{true; tree(x)}
  void flatten_tree(loc x)
{true ; lseg(x, y) }

###

void flatten_tree (loc x) {
  if (x == 0) {
  } else {
    let l2 = *(x + 1);
    let r2 = *(x + 2);
    free(x);
    flatten_tree(r2);
    flatten_tree(l2);
  }
}