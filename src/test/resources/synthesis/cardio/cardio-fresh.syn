should be able to synthesize list copy with omitted cardinalities 

#####

{true ; r :-> x ** sll_card(x, s)}
void sll_copy(loc r)
{b <= a /\ a <= b ; r :-> y ** sll_card(x, s) ** sll_card(y, s) }

#####

void sll_copy (loc r) {
  let x = *r;
  if (x == 0) {
  } else {
    let v = *x;
    let n = *(x + 1);
    *x = n;
    sll_copy(x);
    let yx = *x;
    let y = malloc(2);
    *r = y;
    *(x + 1) = yx;
    *(y + 1) = n;
    *x = v;
    *y = v;
  }
}