should be able to handle simple assertions about integers

###

{ (x < y) /\ (y <= z) ; r :-> a }

void foo(loc r, int x, int y, int z)

{ ((x <= m) /\ (y <= m)) /\ (z <= m) ; r :-> m }

###

void foo (loc r, int x, int y, int z) {
  *r = z;
}