should be able to synthesize list copy

#####

{true ; r :-> x ** lseg(x, 0)}
  void free_llist(loc x, loc r)
{true ; r :-> x ** lseg(x, 0) }

###

void free_llist (loc x, loc r) {
  if (x == 0) {
  } else {
  }
}