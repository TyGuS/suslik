sorted list: insert an element

#####

{0 == n  /\  0 <= k /\  k <= 7  /\  lo1 == 7  /\  hi1 == 0 ; r :-> a }
void srtl_insert (loc x, int k, loc r)
{n + 1 == 1 + len1  /\  0 <= len1  /\  (k <= lo ? k : lo) == (v <= lo1 ? v : lo1)  /\  hi == (hi1 <= v ? v : hi1) /\ v <= lo1;
                                         [y, 2] ** y :-> v ** (y + 1) :-> nxt ** srtl(nxt, len1, lo1, hi1) ** r :-> y }
#####

void srtl_insert (loc x, int k, loc r) {
  let y2 = malloc(2);
  *y2 = k;
  *r = y2;
  *(y2 + 1) = 0;
}