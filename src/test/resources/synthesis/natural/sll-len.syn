should be able to synthesize list length

#####

{0 <= n ; r :-> a ** sll(x, n, lo, hi) }
void list_len(loc x, loc r)
{true ; r :-> n ** sll(x, n, lo, hi) }

#####

void list_len (loc x, loc r) {
  if (x == 0) {
    *r = 0;
  } else {
    let nxt2 = *(x + 1);
    list_len(nxt2, r);
    let len12 = *r;
    *r = 1 + len12;
  }
}