should be able to initialize a linked list
###

{ sll(x, s, mut, imm, imm) }
  void sll_init(loc x, int v)
{ s1 <= {v} ; sll(x, s1, mut, imm, imm) }

###

void sll_init (loc x, int v) {
  if (x == 0) {
  } else {
    let n = *(x + 1);
    sll_init(n, v);
    *x = v;
  }
}