should be able to synthesize list copy (implemented with sequences)

#####

{true ; r :-> x ** sll(x, s)<a>}
void sll_copy(loc r)
{true ; r :-> y ** sll(x, s)<a> ** sll(y, s)<b> }

#####

void sll_copy (loc r) {
  let x = *r;
  if (x == 0) {
  } else {
    let v = *x;
    let n = *(x + 1);
    *r = n;
    sll_copy(r);
    let y1 = *r;
    let y = malloc(2);
    *r = y;
    *(y + 1) = y1;
    *y = v;
  }
}