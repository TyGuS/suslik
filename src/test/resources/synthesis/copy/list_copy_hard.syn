should be able to synthesize list copy

#####

{true ; r :-> x ** lseg(x, 0, S)}
void listcopy(loc r, loc x)
{true ; r :-> y ** lseg(x, 0, S) ** lseg(y, 0, S) }

#####

void listcopy (loc r, loc x) {
  if (x == 0) {
  } else {
    let v2 = *x;
    let nxt2 = *(x + 1);
    *r = nxt2;
    listcopy(r, nxt2);
    let y12 = *r;
    let y2 = malloc(2);
    *y2 = v2;
    *(y2 + 1) = y12;
    *r = y2;
  }
}