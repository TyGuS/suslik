should be able to synthesize tree size from the root pointer

#####


{0 <= n ; r :-> x ** treeN(x, n) }
void tree_size(loc r)
{true ; r :-> n ** treeN(x, n) }

#####

void tree_size (loc r) {
  let x2 = *r;
  if (x2 == 0) {
  } else {
    let l2 = *(x2 + 1);
    let r12 = *(x2 + 2);
    *x2 = l2;
    tree_size(x2);
    let n12 = *x2;
    *r = r12;
    tree_size(r);
    let n22 = *r;
    *r = 1 + n12 + n22;
  }
}