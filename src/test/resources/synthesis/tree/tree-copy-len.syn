should be able to synthesize a tree copy (with preserving length)

#####

{true ; r :-> 0 ** treeN(x, n)<a>}
void tree_copy(loc x, loc r)
{true ; r :-> y ** treeN(x, n)<a> ** treeN(y, n)<a> }

#####

void tree_copy (loc x, loc r) {
  if (x == 0) {
  } else {
    let l = *(x + 1);
    let rx = *(x + 2);
    tree_copy(l, r);
    let yl = *r;
    *r = 0;
    tree_copy(rx, r);
    let yr = *r;
    let y = malloc(3);
    *(x + 1) = yl;
    *r = y;
    *(y + 1) = l;
    *(y + 2) = yr;
  }
}