should be able to flatten a tree (this is actually just tree-free again)
###

{true; tree0(x)}
  void flatten_tree(loc x)
{true ; lsegN(x, y, len) }

###

void flatten_tree (loc x) {
  if (x == 0) {
  } else {
    let l2 = *(x + 1);
    let r2 = *(x + 2);
    flatten_tree(l2);
    flatten_tree(r2);
    free(x);
  }
}