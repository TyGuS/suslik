should be able to synthesize tree copy

#####

{0 <= n ; r :-> a ** tree_size(x, n)}
void tree_copy(loc r, loc x)
{true ; r :-> y ** tree_size(x, n) ** tree_size(y, n) }

#####

void list_copy (loc r, loc x) {
  if (x == 0) {
    *r = 0;
  } else {
    let nxt2 = *(x + 1);
    let nxt3 = *(x + 2);
    tree_copy(r, nxt2);
    let y12 = *r;
    tree_copy(r, nxt3);
    let y13 = *r;
    let y2 = malloc(3);
    *(y2 + 1) = y12;
    *(y2 + 2) = y13;
    *r = y2;
  }
}