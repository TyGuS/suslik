predicate tree_size(x, sz) {
|  x == 0        => { sz == 0 ; emp }
|  not (x == 0)  => { sz == 1 + szl + szr  /\  0 <= szl  /\  0 <= szr ; [x, 3] ** x :-> e ** (x + 1) :-> l ** (x + 2) :-> r ** tree_size(l, szl) ** tree_size(r, szr) }
}
